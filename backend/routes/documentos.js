const express = require('express');
const router = express.Router();

// Implementado en Fase 1
// GET /api/documentos
// GET /api/documentos/:departamento
// POST /api/documentos
// DELETE /api/documentos/:id

module.exports = router;
