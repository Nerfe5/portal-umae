# En desarrollo local: apunta al backend local
PUBLIC_BACKEND_URL=http://localhost:3001

# En producción (mismo servidor): cadena vacía
# El frontend se sirve desde el mismo origen que el backend
# PUBLIC_BACKEND_URL=
